# Etch A Sketch

This is a browser version of a simple sketchpad built with HTML, CSS and JavaScript. 

Main features:
- Draw by hovering over the grid squares
- Randomisation of the squares' RGB values with each interaction
- Button for clearing the grid
- Creation of new grid (up to 100 squares per side)